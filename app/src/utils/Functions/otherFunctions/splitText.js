export default function splitText(splitText, splitFunction) {
  return new Promise(resolve => {
    const amount = splitText;
    if (true) {
      const title = amount.split('-');

      resolve(title[0]);
    }
  });
}
